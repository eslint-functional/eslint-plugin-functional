var x = 0;

for(const x = 0; x < 10; x++) { console.log(x); }
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ [failure]

for(const x in y) { console.log(x); }
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ [failure]

for(const x of y) { console.log(x); }
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ [failure]

while(1 === 1) { console.log("a"); }
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ [failure]

do { console.log("a"); } while(1 === 1)
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ [failure]

[failure]: Unexpected loop, use map or reduce instead.
