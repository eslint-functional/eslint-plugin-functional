const x;

const x = 0;


let mutable;
let mutable = 0;


for(const x = 0; x < 1;x++);

for(const x = 0, y = 0; x < 1;x++);

for(const x in y);

for(const x of y);

for(let mutable = 0; x < 1;x++);

for(const mutable = 0, y = 0; x < 1;x++);

for(let mutable in y);

for(let mutable of y);


function foo() {
  const x;
  const x = 0;
  let mutable;
  let mutable = 0;
}

const foo = () => {
  const x;
  const x = 0;
  let mutable;
  let mutable = 0;
}

class Foo {

  // Class members do not use let
  x;
  x = 0;

  foo() {
    const x;
    const x = 0;
    let mutable;
    let mutable = 0;
  }

}

