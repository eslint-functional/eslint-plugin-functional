interface Foo {
  bar(a: number, b: string): number;
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ [failure]
}

type Foo2 = {
  bar(a: number, b: string): number
  ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~ [failure]
}

[failure]: Method signature is mutable, use property signature with readonly modifier instead.
