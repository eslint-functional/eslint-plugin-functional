const x;

const x = 0;


let mutableX;
let mutableX = 0;


for(const x = 0; x < 1;x++);

for(const x = 0, y = 0; x < 1;x++);

for(const x in y);

for(const x of y);

for(let mutableX = 0; x < 1;x++);

for(const mutableX = 0, y = 0; x < 1;x++);

for(let mutableX = 0, mutableY = 0; x < 1;x++);

for(let mutableX in y);

for(let mutableX of y);


function foo() {
  const x;
  const x = 0;
  let mutableX;
  let mutableX = 0;
}

const foo = () => {
  const x;
  const x = 0;
  let mutableX;
  let mutableX = 0;
}

class Foo {

  // Class members do not use let
  x;
  x = 0;

  foo() {
    const x;
    const x = 0;
    let mutableX;
    let mutableX = 0;
  }

}

