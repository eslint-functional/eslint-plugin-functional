{
  "extends": "./configs/base.json",
  "rules": {
    "no-param-reassign": "error",
    "no-undef": "error",
    "no-var": "error",
    "prefer-const": "error"
  },
  "overrides": [
    {
      "files": ["*.ts", "*.tsx"],
      "rules": {}
    }
  ]
}
