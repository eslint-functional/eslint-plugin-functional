class Foo {

  foo: number;
  ~~~~~~~~~~~~ [failure]

  private foo: number;
  ~~~~~~~~~~~~~~~~~~~~ [failure]

  static foo: number;
  ~~~~~~~~~~~~~~~~~~~ [failure]

  @decorator() foo: number;
  ~~~~~~~~~~~~~~~~~~~~~~~~~ [failure]
}

[failure]: A readonly modifier is required.
