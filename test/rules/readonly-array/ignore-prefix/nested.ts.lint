const _2DArray: number[][] = [[0], [1], [2]];
                ~~~~~~~~~~                    [failure]
                ~~~~~~~~                      [failure]

const mutable2DArray: number[][] = [[0], [1], [2]];

const mutableObjectArray: { foo: number[] } = { foo: [0] };

[failure]: Only ReadonlyArray allowed.
