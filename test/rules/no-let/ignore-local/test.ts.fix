const x;

const x = 0;

for(const x = 0; x < 1;x++);

for(const x = 0, y = 0; x < 1;x++);

for(const x in y);

for(const x of y);

function foo() {
  let x;
  let x = 0;
}

const foo = () => {
  let x;
  let x = 0;
}

class Foo {

  // Class members do not use let
  x;
  x = 0;

  foo() {
    let x;
    let x = 0;
  }

}

